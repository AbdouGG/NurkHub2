--[[
    Premium Modern UI Library
    Features: Gradients, Strokes, Smooth Animations, Mobile Friendly
    Author: Generated by Gemini
]]

local Library = {}
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

--// Premium Theme Colors
local Theme = {
    Background = Color3.fromRGB(20, 20, 20),
    Topbar = Color3.fromRGB(30, 30, 30),
    Section = Color3.fromRGB(30, 30, 30),
    Element = Color3.fromRGB(35, 35, 35),
    ElementStroke = Color3.fromRGB(60, 60, 60),
    Text = Color3.fromRGB(245, 245, 245),
    TextDark = Color3.fromRGB(170, 170, 170),
    Accent = Color3.fromRGB(0, 140, 255),
    AccentGradient = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 140, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 110, 200))
    },
    ToggleOn = Color3.fromRGB(46, 204, 113),
    ToggleOff = Color3.fromRGB(231, 76, 60)
}

--// Utility: Tween
local function Tween(object, info, properties)
    local tween = TweenService:Create(object, TweenInfo.new(info, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), properties)
    tween:Play()
    return tween
end

--// Utility: Draggable
local function MakeDraggable(topbarobject, object)
    local Dragging, DragInput, DragStart, StartPosition

    local function Update(input)
        local Delta = input.Position - DragStart
        local pos = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + Delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + Delta.Y)
        Tween(object, 0.05, {Position = pos})
    end

    topbarobject.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            Dragging = true
            DragStart = input.Position
            StartPosition = object.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    Dragging = false
                end
            end)
        end
    end)

    topbarobject.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            DragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == DragInput and Dragging then
            Update(input)
        end
    end)
end

--// Utility: Create Stroke
local function AddStroke(parent, color, thickness)
    local stroke = Instance.new("UIStroke")
    stroke.Parent = parent
    stroke.Color = color or Theme.ElementStroke
    stroke.Thickness = thickness or 1
    stroke.Transparency = 0.5
    stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    return stroke
end

function Library:CreateWindow(name)
    local WindowTable = {}
    
    -- Main ScreenGui
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "PremiumLib_" .. name
    if gethui then ScreenGui.Parent = gethui()
    elseif CoreGui:FindFirstChild("RobloxGui") then ScreenGui.Parent = CoreGui
    else ScreenGui.Parent = Player:WaitForChild("PlayerGui") end
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    -- Mobile Toggle Button
    local OpenBtn = Instance.new("TextButton")
    OpenBtn.Name = "OpenButton"
    OpenBtn.Parent = ScreenGui
    OpenBtn.BackgroundColor3 = Theme.Topbar
    OpenBtn.Position = UDim2.new(0.05, 0, 0.1, 0)
    OpenBtn.Size = UDim2.new(0, 45, 0, 45)
    OpenBtn.Text = ""
    Instance.new("UICorner", OpenBtn).CornerRadius = UDim.new(0, 8)
    AddStroke(OpenBtn, Theme.Accent, 2)
    MakeDraggable(OpenBtn, OpenBtn)

    local OpenIcon = Instance.new("ImageLabel")
    OpenIcon.Parent = OpenBtn
    OpenIcon.BackgroundTransparency = 1
    OpenIcon.Position = UDim2.new(0.2, 0, 0.2, 0)
    OpenIcon.Size = UDim2.new(0.6, 0, 0.6, 0)
    OpenIcon.Image = "rbxassetid://3926305904"
    OpenIcon.ImageRectOffset = Vector2.new(764, 244) -- Gear/Menu icon
    OpenIcon.ImageRectSize = Vector2.new(36, 36)
    OpenIcon.ImageColor3 = Theme.Accent

    -- Main Frame
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = Theme.Background
    MainFrame.Position = UDim2.new(0.5, -225, 0.5, -175)
    MainFrame.Size = UDim2.new(0, 450, 0, 350)
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)
    AddStroke(MainFrame, Theme.Accent, 2)

    -- Toggle Logic
    local isOpen = true
    OpenBtn.MouseButton1Click:Connect(function()
        isOpen = not isOpen
        if isOpen then
            MainFrame.Visible = true
            Tween(MainFrame, 0.3, {Size = UDim2.new(0, 450, 0, 350)})
        else
            Tween(MainFrame, 0.3, {Size = UDim2.new(0, 450, 0, 0)})
            task.wait(0.3)
            if not isOpen then MainFrame.Visible = false end
        end
    end)

    -- Topbar
    local Topbar = Instance.new("Frame")
    Topbar.Name = "Topbar"
    Topbar.Parent = MainFrame
    Topbar.BackgroundColor3 = Theme.Topbar
    Topbar.Size = UDim2.new(1, 0, 0, 40)
    Topbar.BorderSizePixel = 0
    Instance.new("UICorner", Topbar).CornerRadius = UDim.new(0, 8)
    
    local TopbarFix = Instance.new("Frame") -- Square off bottom corners
    TopbarFix.Parent = Topbar
    TopbarFix.BackgroundColor3 = Theme.Topbar
    TopbarFix.BorderSizePixel = 0
    TopbarFix.Position = UDim2.new(0, 0, 1, -5)
    TopbarFix.Size = UDim2.new(1, 0, 0, 5)

    local TopGradient = Instance.new("UIGradient")
    TopGradient.Parent = Topbar
    TopGradient.Rotation = 90
    TopGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(40,40,40)), 
        ColorSequenceKeypoint.new(1, Theme.Topbar)
    }

    local Title = Instance.new("TextLabel")
    Title.Parent = Topbar
    Title.BackgroundTransparency = 1
    Title.Position = UDim2.new(0, 15, 0, 0)
    Title.Size = UDim2.new(1, -30, 1, 0)
    Title.Font = Enum.Font.GothamBold
    Title.Text = name
    Title.TextColor3 = Theme.Text
    Title.TextSize = 16
    Title.TextXAlignment = Enum.TextXAlignment.Left

    MakeDraggable(Topbar, MainFrame)

    -- Tab Container
    local TabContainer = Instance.new("ScrollingFrame")
    TabContainer.Parent = MainFrame
    TabContainer.Active = true
    TabContainer.BackgroundColor3 = Theme.Background
    TabContainer.BackgroundTransparency = 1
    TabContainer.Position = UDim2.new(0, 0, 0, 45)
    TabContainer.Size = UDim2.new(1, 0, 0, 32)
    TabContainer.CanvasSize = UDim2.new(0,0,0,0)
    TabContainer.ScrollBarThickness = 0
    
    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.Parent = TabContainer
    TabListLayout.FillDirection = Enum.FillDirection.Horizontal
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 5)
    
    local TabPadding = Instance.new("UIPadding")
    TabPadding.Parent = TabContainer
    TabPadding.PaddingLeft = UDim.new(0, 10)

    local Pages = Instance.new("Frame")
    Pages.Parent = MainFrame
    Pages.BackgroundTransparency = 1
    Pages.Position = UDim2.new(0, 0, 0, 85)
    Pages.Size = UDim2.new(1, 0, 1, -90)

    function WindowTable:Tab(tabName)
        local TabObj = {}
        
        local TabBtn = Instance.new("TextButton")
        TabBtn.Parent = TabContainer
        TabBtn.BackgroundColor3 = Theme.Element
        TabBtn.Text = tabName
        TabBtn.Font = Enum.Font.GothamBold
        TabBtn.TextColor3 = Theme.TextDark
        TabBtn.TextSize = 13
        TabBtn.AutoButtonColor = false
        Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 6)
        AddStroke(TabBtn, Theme.ElementStroke, 1)

        local txtSize = game:GetService("TextService"):GetTextSize(tabName, 13, Enum.Font.GothamBold, Vector2.new(1000, 32))
        TabBtn.Size = UDim2.new(0, txtSize.X + 24, 1, 0)
        TabContainer.CanvasSize = UDim2.new(0, TabListLayout.AbsoluteContentSize.X + 20, 0, 0)

        local Page = Instance.new("ScrollingFrame")
        Page.Parent = Pages
        Page.BackgroundTransparency = 1
        Page.Size = UDim2.new(1, 0, 1, 0)
        Page.ScrollBarThickness = 2
        Page.ScrollBarImageColor3 = Theme.Accent
        Page.Visible = false

        local PageLayout = Instance.new("UIListLayout")
        PageLayout.Parent = Page
        PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageLayout.Padding = UDim.new(0, 6)
        
        local PagePadding = Instance.new("UIPadding")
        PagePadding.Parent = Page
        PagePadding.PaddingLeft = UDim.new(0, 10)
        PagePadding.PaddingRight = UDim.new(0, 10)
        PagePadding.PaddingTop = UDim.new(0, 5)

        PageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            Page.CanvasSize = UDim2.new(0, 0, 0, PageLayout.AbsoluteContentSize.Y + 10)
        end)

        TabBtn.MouseButton1Click:Connect(function()
            for _, v in pairs(TabContainer:GetChildren()) do
                if v:IsA("TextButton") then
                    Tween(v, 0.2, {BackgroundColor3 = Theme.Element, TextColor3 = Theme.TextDark})
                    v.UIStroke.Color = Theme.ElementStroke
                end
            end
            for _, v in pairs(Pages:GetChildren()) do v.Visible = false end
            
            Tween(TabBtn, 0.2, {BackgroundColor3 = Theme.Accent, TextColor3 = Theme.Text})
            TabBtn.UIStroke.Color = Theme.Accent
            Page.Visible = true
        end)

        if #Pages:GetChildren() == 1 then
            TabBtn.BackgroundColor3 = Theme.Accent
            TabBtn.TextColor3 = Theme.Text
            TabBtn.UIStroke.Color = Theme.Accent
            Page.Visible = true
        end

        --// Helper: Tooltip
        local function AddTooltip(parent, text)
            if not text then return end
            
            local InfoIcon = Instance.new("ImageButton")
            InfoIcon.Parent = parent
            InfoIcon.BackgroundTransparency = 1
            InfoIcon.Position = UDim2.new(1, -24, 0, 8)
            InfoIcon.Size = UDim2.new(0, 18, 0, 18)
            InfoIcon.Image = "rbxassetid://3926305904"
            InfoIcon.ImageRectOffset = Vector2.new(524, 444)
            InfoIcon.ImageRectSize = Vector2.new(36, 36)
            InfoIcon.ImageColor3 = Theme.TextDark

            local TipFrame = Instance.new("Frame")
            TipFrame.Parent = parent.Parent
            TipFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
            TipFrame.Size = UDim2.new(0.9, 0, 0, 0)
            TipFrame.Visible = false
            TipFrame.ClipsDescendants = true
            TipFrame.LayoutOrder = parent.LayoutOrder + 1
            Instance.new("UICorner", TipFrame).CornerRadius = UDim.new(0, 6)
            AddStroke(TipFrame, Theme.Accent, 1)

            local TipText = Instance.new("TextLabel")
            TipText.Parent = TipFrame
            TipText.BackgroundTransparency = 1
            TipText.Size = UDim2.new(1, -10, 1, 0)
            TipText.Position = UDim2.new(0, 5, 0, 0)
            TipText.Font = Enum.Font.Gotham
            TipText.Text = text
            TipText.TextColor3 = Theme.Text
            TipText.TextSize = 12
            TipText.TextWrapped = true

            local open = false
            InfoIcon.MouseButton1Click:Connect(function()
                open = not open
                if open then
                    TipFrame.Visible = true
                    Tween(TipFrame, 0.2, {Size = UDim2.new(1, 0, 0, 35)})
                    Tween(InfoIcon, 0.2, {ImageColor3 = Theme.Accent})
                else
                    Tween(TipFrame, 0.2, {Size = UDim2.new(1, 0, 0, 0)})
                    Tween(InfoIcon, 0.2, {ImageColor3 = Theme.TextDark})
                    task.wait(0.2)
                    if not open then TipFrame.Visible = false end
                end
            end)
        end

        function TabObj:Section(text)
            local SecTitle = Instance.new("TextLabel")
            SecTitle.Parent = Page
            SecTitle.BackgroundTransparency = 1
            SecTitle.Size = UDim2.new(1, 0, 0, 20)
            SecTitle.Font = Enum.Font.GothamBold
            SecTitle.Text = text
            SecTitle.TextColor3 = Theme.Accent
            SecTitle.TextSize = 12
            SecTitle.TextXAlignment = Enum.TextXAlignment.Left
        end

        function TabObj:Button(text, info, callback)
            local Btn = Instance.new("TextButton")
            Btn.Parent = Page
            Btn.BackgroundColor3 = Theme.Element
            Btn.Size = UDim2.new(1, 0, 0, 34)
            Btn.Font = Enum.Font.GothamSemibold
            Btn.Text = "  " .. text
            Btn.TextColor3 = Theme.Text
            Btn.TextSize = 14
            Btn.TextXAlignment = Enum.TextXAlignment.Left
            Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 6)
            AddStroke(Btn)
            AddTooltip(Btn, info)

            Btn.MouseButton1Click:Connect(function()
                Tween(Btn, 0.1, {BackgroundColor3 = Theme.Accent})
                task.wait(0.1)
                Tween(Btn, 0.1, {BackgroundColor3 = Theme.Element})
                callback()
            end)
        end

        function TabObj:Toggle(text, info, default, callback)
            local toggled = default or false
            
            local TglFrame = Instance.new("TextButton")
            TglFrame.Parent = Page
            TglFrame.BackgroundColor3 = Theme.Element
            TglFrame.Size = UDim2.new(1, 0, 0, 34)
            TglFrame.Text = ""
            TglFrame.AutoButtonColor = false
            Instance.new("UICorner", TglFrame).CornerRadius = UDim.new(0, 6)
            AddStroke(TglFrame)

            local Label = Instance.new("TextLabel")
            Label.Parent = TglFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 10, 0, 0)
            Label.Size = UDim2.new(0.6, 0, 1, 0)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left

            -- Pill Background
            local SwitchBg = Instance.new("Frame")
            SwitchBg.Parent = TglFrame
            SwitchBg.BackgroundColor3 = toggled and Theme.ToggleOn or Theme.ElementStroke
            SwitchBg.Position = UDim2.new(1, -50, 0.5, -10)
            SwitchBg.Size = UDim2.new(0, 40, 0, 20)
            Instance.new("UICorner", SwitchBg).CornerRadius = UDim.new(1, 0)

            -- Circle Knob
            local Knob = Instance.new("Frame")
            Knob.Parent = SwitchBg
            Knob.BackgroundColor3 = Theme.Text
            Knob.Position = toggled and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
            Knob.Size = UDim2.new(0, 16, 0, 16)
            Instance.new("UICorner", Knob).CornerRadius = UDim.new(1, 0)

            AddTooltip(TglFrame, info)

            TglFrame.MouseButton1Click:Connect(function()
                toggled = not toggled
                if toggled then
                    Tween(SwitchBg, 0.2, {BackgroundColor3 = Theme.ToggleOn})
                    Tween(Knob, 0.2, {Position = UDim2.new(1, -18, 0.5, -8)})
                else
                    Tween(SwitchBg, 0.2, {BackgroundColor3 = Theme.ElementStroke})
                    Tween(Knob, 0.2, {Position = UDim2.new(0, 2, 0.5, -8)})
                end
                callback(toggled)
            end)
        end

        function TabObj:Slider(text, info, min, max, default, callback)
            local Value = default or min
            
            local SldFrame = Instance.new("Frame")
            SldFrame.Parent = Page
            SldFrame.BackgroundColor3 = Theme.Element
            SldFrame.Size = UDim2.new(1, 0, 0, 45)
            Instance.new("UICorner", SldFrame).CornerRadius = UDim.new(0, 6)
            AddStroke(SldFrame)

            local Label = Instance.new("TextLabel")
            Label.Parent = SldFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 10, 0, 4)
            Label.Size = UDim2.new(1, -20, 0, 20)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left

            local ValText = Instance.new("TextLabel")
            ValText.Parent = SldFrame
            ValText.BackgroundTransparency = 1
            ValText.Position = UDim2.new(1, -60, 0, 4)
            ValText.Size = UDim2.new(0, 50, 0, 20)
            ValText.Font = Enum.Font.Gotham
            ValText.Text = tostring(Value)
            ValText.TextColor3 = Theme.TextDark
            ValText.TextSize = 13
            ValText.TextXAlignment = Enum.TextXAlignment.Right

            -- Slider Bg
            local BarBg = Instance.new("TextButton")
            BarBg.Name = "Bar"
            BarBg.Parent = SldFrame
            BarBg.BackgroundColor3 = Color3.fromRGB(20,20,20)
            BarBg.Position = UDim2.new(0, 10, 0, 30)
            BarBg.Size = UDim2.new(1, -20, 0, 4)
            BarBg.Text = ""
            BarBg.AutoButtonColor = false
            Instance.new("UICorner", BarBg).CornerRadius = UDim.new(1, 0)

            local Fill = Instance.new("Frame")
            Fill.Parent = BarBg
            Fill.BackgroundColor3 = Theme.Accent
            Fill.Size = UDim2.new((Value - min) / (max - min), 0, 1, 0)
            Instance.new("UICorner", Fill).CornerRadius = UDim.new(1, 0)
            
            local Knob = Instance.new("Frame")
            Knob.Parent = Fill
            Knob.BackgroundColor3 = Theme.Text
            Knob.Position = UDim2.new(1, -6, 0.5, -6)
            Knob.Size = UDim2.new(0, 12, 0, 12)
            Instance.new("UICorner", Knob).CornerRadius = UDim.new(1, 0)
            AddStroke(Knob, Theme.Accent, 1)

            AddTooltip(SldFrame, info)

            local dragging = false
            local function Update(input)
                local SizeX = math.clamp((input.Position.X - BarBg.AbsolutePosition.X) / BarBg.AbsoluteSize.X, 0, 1)
                local NewValue = math.floor(min + ((max - min) * SizeX))
                
                Tween(Fill, 0.05, {Size = UDim2.new(SizeX, 0, 1, 0)})
                ValText.Text = tostring(NewValue)
                callback(NewValue)
            end

            BarBg.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    Update(input)
                end
            end)
            
            UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    Update(input)
                end
            end)
            
            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = false
                end
            end)
        end

        function TabObj:Dropdown(text, info, options, callback)
            local isDropped = false
            
            local DropFrame = Instance.new("Frame")
            DropFrame.Parent = Page
            DropFrame.BackgroundColor3 = Theme.Element
            DropFrame.Size = UDim2.new(1, 0, 0, 34)
            DropFrame.ClipsDescendants = true
            Instance.new("UICorner", DropFrame).CornerRadius = UDim.new(0, 6)
            AddStroke(DropFrame)
            
            local DropBtn = Instance.new("TextButton")
            DropBtn.Parent = DropFrame
            DropBtn.BackgroundTransparency = 1
            DropBtn.Size = UDim2.new(1, 0, 0, 34)
            DropBtn.Font = Enum.Font.GothamSemibold
            DropBtn.Text = "  " .. text
            DropBtn.TextColor3 = Theme.Text
            DropBtn.TextSize = 14
            DropBtn.TextXAlignment = Enum.TextXAlignment.Left

            local Arrow = Instance.new("ImageLabel")
            Arrow.Parent = DropBtn
            Arrow.BackgroundTransparency = 1
            Arrow.Position = UDim2.new(1, -25, 0.5, -8)
            Arrow.Size = UDim2.new(0, 16, 0, 16)
            Arrow.Image = "rbxassetid://6034818372" -- Down Arrow
            Arrow.ImageColor3 = Theme.TextDark

            AddTooltip(DropFrame, info)
            
            local OptionContainer = Instance.new("Frame")
            OptionContainer.Parent = DropFrame
            OptionContainer.BackgroundTransparency = 1
            OptionContainer.Position = UDim2.new(0, 0, 0, 34)
            OptionContainer.Size = UDim2.new(1, 0, 0, 0)
            
            local OptionList = Instance.new("UIListLayout")
            OptionList.Parent = OptionContainer
            OptionList.SortOrder = Enum.SortOrder.LayoutOrder
            
            local function Refresh()
                for _, v in pairs(OptionContainer:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
                for _, opt in pairs(options) do
                    local OptBtn = Instance.new("TextButton")
                    OptBtn.Parent = OptionContainer
                    OptBtn.BackgroundColor3 = Color3.fromRGB(28,28,28)
                    OptBtn.Size = UDim2.new(1, 0, 0, 30)
                    OptBtn.Font = Enum.Font.Gotham
                    OptBtn.Text = opt
                    OptBtn.TextColor3 = Theme.TextDark
                    OptBtn.TextSize = 13
                    
                    OptBtn.MouseButton1Click:Connect(function()
                        DropBtn.Text = "  " .. text .. ": " .. opt
                        isDropped = false
                        Tween(DropFrame, 0.2, {Size = UDim2.new(1, 0, 0, 34)})
                        Tween(Arrow, 0.2, {Rotation = 0})
                        callback(opt)
                    end)
                end
            end
            Refresh()
            
            DropBtn.MouseButton1Click:Connect(function()
                isDropped = not isDropped
                if isDropped then
                    local count = #options
                    Tween(DropFrame, 0.2, {Size = UDim2.new(1, 0, 0, 34 + (count * 30))})
                    Tween(Arrow, 0.2, {Rotation = 180})
                else
                    Tween(DropFrame, 0.2, {Size = UDim2.new(1, 0, 0, 34)})
                    Tween(Arrow, 0.2, {Rotation = 0})
                end
            end)
        end

        return TabObj
    end

    return WindowTable
end

return Library
